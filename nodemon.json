{
  "watch": "src",
  "ext": "ts,js,json",
  "ignore": ["node_modules", "coverage", "dist"],
  "exec": "tsc --build tsconfig.prod.json && node ./dist/src/app.js",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
